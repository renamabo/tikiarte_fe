<div id="header">
  <h1 class="text-center">Public Gallery</h1>
</div>

<% if @public_images.nil? == false %>
  <div id="images">
    <div class="row">
      <% @public_images.each do |image| %>
      <div class="col-lg-4 mb-4 mb-lg-0">
        <img
          src="<%= image.url %>"
          class="w-100 shadow-1-strong rounded mb-4"
          alt="<%= image.title %>"
        />
      </div>
      <% end %>
    </div>
  </div>
<% end %>
